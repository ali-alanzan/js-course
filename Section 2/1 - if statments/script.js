// if statments
/**
 * if( condition ) {
 *  // codes
 * }
 * 
 * condition always must be contains a true value
 */
var isMonday = false;

var condition = isMonday;

if ( condition ) { // start scope
    // scope for the if function

    // statments, codes

    console.log('Hello, Nice Day');
    alert('okay, alert from the if condition');
} // end scope